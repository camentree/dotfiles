# Manual Setup

1. Install 1Password
   a. unset `cmd + \` shortcut (Settings -> General -> Keyboard Shorcuts -> Autofill)
2. Install VSCode
3. `pip install pandas numpy ipywidgets matplotlib seaborn tqdm`
